# Деплой бота на Render

## Что было добавлено

В проект добавлен **веб-сервер** (`web_server.py`), который:
- ✅ Слушает порт для крон-джоба Render
- ✅ Предоставляет `/health` endpoint для health checks
- ✅ Работает параллельно с Telegram ботом
- ✅ НЕ влияет на функциональность бота

## Как это работает

1. **Веб-сервер** (aiohttp) запускается на порту, указанном в переменной `PORT` (по умолчанию 8080)
2. **Telegram бот** работает в режиме polling (получает обновления напрямую от Telegram)
3. Оба сервиса работают **одновременно** в одном процессе благодаря asyncio

## Endpoints

- `GET /` - корневая страница (возвращает "Bot is running!")
- `GET /health` - health check для Render (возвращает JSON со статусом)

## Деплой на Render

### Шаг 1: Подготовка репозитория
```bash
git add .
git commit -m "Add web server for Render deployment"
git push
```

### Шаг 2: Создание сервиса на Render

1. Зайдите на https://render.com
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub/GitLab репозиторий
4. Render автоматически обнаружит `render.yaml` и применит настройки

### Шаг 3: Настройка переменных окружения

В панели Render добавьте переменные окружения:
- `BOT_TOKEN` - токен вашего Telegram бота (от @BotFather)
- `GROQ_API_KEY` - ключ API Groq для гороскопов
- `GROQ_MODEL` - модель Groq (например, `llama-3.3-70b-versatile`)

### Шаг 4: Деплой

Render автоматически:
1. Установит зависимости из `requirements.txt`
2. Запустит `python main.py`
3. Начнёт проверять `/health` endpoint каждые 30 секунд

## Крон-джоб для пробуждения

Render усыпляет бесплатные сервисы после 15 минут бездействия. Чтобы бот работал 24/7:

### Вариант 1: Использовать Render Cron Jobs (рекомендуется)

1. В Render создайте "Cron Job"
2. Команда: `curl https://ваш-сервис.onrender.com/health`
3. Расписание: `*/5 * * * *` (каждые 5 минут)

### Вариант 2: Внешний крон-джоб

Используйте сервисы типа:
- **cron-job.org** (бесплатно)
- **UptimeRobot** (бесплатно)
- **Healthchecks.io** (бесплатно)

Настройте GET-запрос на:
```
https://ваш-сервис.onrender.com/health
```
каждые 5 минут.

## Проверка работоспособности

После деплоя проверьте:

1. **Логи в Render**: должны увидеть:
   ```
   🌐 Веб-сервер запущен на порту 8080
   ✅ Health check доступен на http://0.0.0.0:8080/health
   🤖 Запуск бота в режиме polling...
   ✅ Бот и веб-сервер успешно запущены
   ```

2. **Health check**: откройте в браузере
   ```
   https://ваш-сервис.onrender.com/health
   ```
   Должен вернуть JSON:
   ```json
   {
     "status": "ok",
     "service": "mystic-bot",
     "message": "Bot is running"
   }
   ```

3. **Telegram бот**: напишите боту `/start` - он должен ответить

## Структура файлов

```
mystic2-main/
├── main.py                 # Основной файл бота (МОДИФИЦИРОВАН)
├── web_server.py          # Новый веб-сервер для Render
├── horoscope_service.py   # Сервис гороскопов
├── matrix_calculator.py   # Калькулятор матрицы
├── interpretations.py     # Интерпретации
├── config.py              # Конфигурация
├── render.yaml            # Конфигурация Render
├── requirements.txt       # Зависимости Python
└── README.md              # Эта инструкция
```

## Что изменилось в main.py

1. **Добавлен импорт**:
   ```python
   from web_server import start_web_server
   import asyncio
   ```

2. **Новая функция `run_bot_with_server()`**:
   - Запускает веб-сервер
   - Запускает бота в режиме polling
   - Управляет жизненным циклом обоих сервисов

3. **Функция `main()` теперь использует**:
   ```python
   asyncio.run(run_bot_with_server(application))
   ```

## Логика работы бота НЕ изменилась

✅ Все функции бота работают **точно так же**:
- Расчёт психоматрицы
- Интерпретации с учётом пола
- Гороскопы на сегодня
- Обработка сообщений
- Все команды и кнопки

Изменился **только способ запуска** - теперь бот запускается вместе с веб-сервером.

## Troubleshooting

### Бот не отвечает
- Проверьте логи в Render
- Убедитесь, что `BOT_TOKEN` правильный
- Проверьте, что сервис не спит (зайдите на `/health`)

### Health check fails
- Проверьте, что порт правильно определяется из `PORT` env var
- Посмотрите логи - должна быть строка "Веб-сервер запущен"

### Бот работает, но засыпает
- Настройте крон-джоб (см. выше)
- Убедитесь, что крон пингует именно `/health` endpoint

## Контакты и поддержка

Если возникли вопросы - проверьте логи в Render Dashboard.

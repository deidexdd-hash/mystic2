# 🤖 Настройка Groq API для улучшенных гороскопов

## 🎯 Зачем нужен Groq API?

**Без Groq:**
- Простая склейка гороскопов из источников
- Нет персонализации
- Возможны повторы и противоречия

**С Groq:**
- 🎨 Красивый персонализированный прогноз
- 📊 Структурированный формат
- 🎯 Учет числа судьбы
- ✨ Мистический стиль изложения

---

## 📝 Шаг 1: Получение API ключа

### 1.1. Регистрация на Groq
1. Перейдите на сайт: https://console.groq.com
2. Нажмите "Sign Up" (если нет аккаунта)
3. Войдите через GitHub/Google или email

### 1.2. Создание API ключа
1. Откройте раздел "API Keys" в меню слева
2. Нажмите "Create API Key"
3. Введите имя ключа (например, "Mystic Bot")
4. **Скопируйте ключ** (он показывается только один раз!)

Пример ключа:
```
gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

## 🔧 Шаг 2: Настройка бота

### Вариант A: Переменные окружения (рекомендуется)

#### Linux/MacOS:
```bash
export GROQ_API_KEY="gsk_your_api_key_here"
export GROQ_MODEL="llama-3.1-8b-instant"
```

Чтобы сохранить постоянно, добавьте в `~/.bashrc` или `~/.zshrc`:
```bash
echo 'export GROQ_API_KEY="gsk_your_api_key_here"' >> ~/.bashrc
echo 'export GROQ_MODEL="llama-3.1-8b-instant"' >> ~/.bashrc
source ~/.bashrc
```

#### Windows (PowerShell):
```powershell
$env:GROQ_API_KEY="gsk_your_api_key_here"
$env:GROQ_MODEL="llama-3.1-8b-instant"
```

Постоянно (в System Properties):
1. Win + R → `sysdm.cpl`
2. Advanced → Environment Variables
3. New → Variable name: `GROQ_API_KEY`
4. Value: ваш ключ

### Вариант B: Файл .env (альтернатива)

1. Создайте файл `.env` в корне проекта:
```bash
touch .env
```

2. Добавьте в него:
```env
BOT_TOKEN=your_telegram_bot_token
GROQ_API_KEY=gsk_your_api_key_here
GROQ_MODEL=llama-3.1-8b-instant
```

3. **Важно!** Добавьте `.env` в `.gitignore`:
```bash
echo ".env" >> .gitignore
```

---

## ✅ Шаг 3: Проверка установки

### 3.1. Установите библиотеку Groq:
```bash
pip install groq
```

### 3.2. Запустите тест:
```bash
python test_horoscope.py
```

### 3.3. Ожидаемый результат:

**Если ключ настроен правильно:**
```
✅ Groq API инициализирован
🤖 Используем AI для генерации
✅ AI-гороскоп сгенерирован (XXX символов)
```

**Если ключ не настроен:**
```
⚠️ GROQ_API_KEY не установлен. AI-функции будут недоступны.
📝 Используем базовую генерацию
```

---

## 🎨 Доступные модели Groq

### Рекомендуемые модели:

#### 1. llama-3.1-8b-instant (рекомендуется)
```env
GROQ_MODEL=llama-3.1-8b-instant
```
- ✅ Быстрая (1-2 секунды)
- ✅ Качественный текст
- ✅ Хорошо следует инструкциям
- ✅ Бесплатная квота

#### 2. mixtral-8x7b-32768
```env
GROQ_MODEL=mixtral-8x7b-32768
```
- ✅ Очень качественный текст
- ⚠️ Чуть медленнее (2-3 секунды)
- ✅ Большой контекст

#### 3. llama-3.3-70b-versatile
```env
GROQ_MODEL=llama-3.3-70b-versatile
```
- ✅ Лучшее качество
- ⚠️ Медленнее (3-5 секунд)
- ⚠️ Расходует квоту быстрее

### Как выбрать?
- **Для большинства случаев:** `llama-3.1-8b-instant`
- **Для максимального качества:** `llama-3.3-70b-versatile`
- **Золотая середина:** `mixtral-8x7b-32768`

---

## 💰 Квоты и лимиты

### Бесплатный план:
- **Запросов в минуту:** 30
- **Запросов в день:** 14,400
- **Токенов в минуту:** 30,000

### Для бота это означает:
- ✅ Примерно **14,400 гороскопов в день** (более чем достаточно!)
- ✅ До 30 пользователей одновременно
- ⚠️ При превышении - ответ через минуту

### Оптимизация:
1. **Кеширование** - один гороскоп на знак в день
2. **Только по запросу** - не генерируем автоматически
3. **Fallback** - при превышении квоты → базовая генерация

---

## 🐛 Решение проблем

### Проблема 1: "API key not found"
```
❌ Ошибка: API key not found
```

**Решение:**
1. Проверьте переменную: `echo $GROQ_API_KEY`
2. Убедитесь, что ключ начинается с `gsk_`
3. Перезапустите терминал после настройки

### Проблема 2: "Rate limit exceeded"
```
❌ Ошибка: Rate limit exceeded
```

**Решение:**
1. Подождите 1 минуту
2. Проверьте квоту на https://console.groq.com
3. Уменьшите частоту запросов

### Проблема 3: "Module not found: groq"
```
ModuleNotFoundError: No module named 'groq'
```

**Решение:**
```bash
pip install groq --upgrade
```

### Проблема 4: Гороскоп не структурирован
```
Текст не соответствует заданному формату
```

**Решение:**
1. Попробуйте другую модель (llama-3.3-70b-versatile)
2. Проверьте, что модель поддерживается
3. Просмотрите логи: может быть ошибка в промпте

---

## 📊 Пример работы с AI vs без AI

### Без AI (базовая генерация):
```
📰 Источник 1: Mail.ru
Сегодня вам стоит обратить внимание на финансовые вопросы...

📰 Источник 2: Rambler
День обещает быть продуктивным, особенно в первой половине...

✨ Совет дня:
Доверяйте своей интуиции и будьте открыты новым возможностям!
```

### С AI (персонализированный):
```
💫 Общий прогноз:
Сегодня звезды благоволят вам в финансовых и карьерных вопросах. 
День обещает быть продуктивным, особенно в первой половине. Ваше 
число судьбы (32) указывает на период активного роста и развития.

❤️ Личная жизнь:
Откройте свое сердце новым знакомствам. Сегодня возможна встреча 
с интересным человеком.

💼 Работа и финансы:
Отличное время для важных переговоров и принятия решений. Доверяйте 
своей интуиции в финансовых вопросах.

🎯 Совет дня:
Не бойтесь рисковать, но оценивайте ситуацию трезво.

⚠️ Предостережение:
Избегайте импульсивных покупок и спонтанных решений до вечера.
```

**Видна разница?** С AI результат:
- ✅ Структурированный
- ✅ Персонализированный
- ✅ Красиво оформленный
- ✅ Учитывает число судьбы

---

## 🚀 Деплой с Groq

### На Render:
1. Откройте настройки вашего сервиса
2. Environment → Add Environment Variable
3. Добавьте:
   - Key: `GROQ_API_KEY`
   - Value: ваш ключ
4. Сохраните и перезапустите

### На Heroku:
```bash
heroku config:set GROQ_API_KEY=gsk_your_key_here
heroku config:set GROQ_MODEL=llama-3.1-8b-instant
```

### Docker:
```bash
docker run -e GROQ_API_KEY=gsk_your_key_here \
           -e GROQ_MODEL=llama-3.1-8b-instant \
           your-bot-image
```

---

## ✨ Итоговый чеклист

- [ ] Зарегистрировались на console.groq.com
- [ ] Получили API ключ
- [ ] Установили groq: `pip install groq`
- [ ] Настроили переменную окружения `GROQ_API_KEY`
- [ ] (Опционально) Настроили `GROQ_MODEL`
- [ ] Запустили тест: `python test_horoscope.py`
- [ ] Проверили, что AI работает
- [ ] Настроили в production (Render/Heroku)

---

## 📚 Дополнительные ресурсы

- 🌐 Groq Console: https://console.groq.com
- 📖 Groq Documentation: https://console.groq.com/docs
- 💬 Groq Discord: https://discord.gg/groq
- 🐛 Issues: https://github.com/groq/groq-python/issues

---

🎉 **Готово! Теперь ваш бот будет создавать магические персонализированные гороскопы!** 🔮
